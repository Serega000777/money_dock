# tests/

Unit-тесты лежат рядом с исходным кодом (`*.test.ts` в пакетах, `*.spec.ts` в `apps/api`).
Здесь — то, что не колокейтится с одним модулем: end-to-end сценарии через реальный API +
Postgres, provider contract tests, миграционные тесты. Появляется вместе с первым модулем,
которому это нужно (Stage 1 — auth e2e).
